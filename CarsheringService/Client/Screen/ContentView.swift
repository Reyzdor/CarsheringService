import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            LoginButton(title: "Login") {
                
            }
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
